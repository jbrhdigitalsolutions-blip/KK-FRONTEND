# KK-FRONTEND v0.2.3 — DESIGN-PACK

- Generates `DESIGN-PACK/` automatically after each successful Reference Scan.
- Keeps a compact root `DESIGN.md` for fast coding-agent use.
- Bundles exact machine JSON, route/profile evidence and captured screenshots.
- Adds `CODING-AGENT-PROMPT.md`, `PACK-README.md` and SHA256 `PACK-MANIFEST.json`.
- Includes actual downloaded assets only when the scan produced them; always includes asset inventory.
- Fixes Machine Contract `anchors: []`; anchors now come from captured profiles.
- Artifacts UI exposes DESIGN-PACK's key handoff files directly.
