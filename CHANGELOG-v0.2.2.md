# KK-FRONTEND v0.2.2 — BLUEPRINT FAST

- Added `Blueprint Fast — fastest + single DESIGN.md`.
- Each design template gets desktop+mobile structural fingerprints.
- Deep evidence is captured only at three anchors: 390×844, 820×1180, 1440×900.
- Exact CSS breakpoint declarations are recorded, but expensive -1/exact/+1 rendered probing is skipped in Blueprint Fast.
- Design assets are inventoried without downloading every asset byte.
- Added automatic self-contained `DESIGN.md` generation after a successful reference scan.
- DESIGN.md contains implementation contract, technology signals, CSS variables, fonts, media/container queries, animations, state rules, route templates, responsive evidence, exact key element geometry/style IDs, interaction/scroll evidence, and design asset inventory.
- Existing `Design Only`, `Fast Deep`, `Standard`, and `Extreme` modes remain available when deeper verification is required.
